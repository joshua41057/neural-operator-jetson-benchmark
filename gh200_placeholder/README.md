# GH200 server-reference measurements

Placeholder. The GH200 sweeps (Section 8.2, Table 9) were run on a Slurm-managed Delta AI
node and are not present on the Jetson host from which this repository was assembled.

To be added: the server-reference harness, the NVML telemetry parser, and the
per-configuration records, under the same `results/` conventions used by `families/`.
